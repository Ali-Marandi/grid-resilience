# Grid Resilience Studio v1.0.0

نسخهٔ `1.0.0`، پروژهٔ اولیهٔ بررسی اتصال‌پذیری را به یک برنامهٔ دسکتاپ ویندوز برای غربال‌گری تاب‌آوری شبکه تبدیل می‌کند. این انتشار بر تحلیل قابل‌تکرار، شفافیت محدودیت‌ها و خروجی قابل‌ممیزی متمرکز است.

| بخش | تغییرات |
|---|---|
| موتور تحلیل | افزوده‌شدن مدل باس/شاخه/ژنراتور، پخش بار DC، کنترل حدود حرارتی، سناریوهای خروج N-1 خط و ژنراتور، تشخیص جزیره‌شدگی و برآورد بارِ بدون‌خدمت |
| رتبه‌بندی | صف اولویت‌دار رخدادها، امتیاز شدت، وضعیت امن/نقض/جزیره‌شدگی/حل‌نشده و شاخص تاب‌آوری |
| رابط ویندوز | داشبورد حرفه‌ای، نقشهٔ شبکه، کارت‌های شاخص، ویرایشگر JSON اعتبارسنجی‌شونده، باز/ذخیرهٔ پروژه و خروجی CSV |
| مهندسی نرم‌افزار | طرح‌وارهٔ نسخه‌پذیر، ذخیره‌سازی اتمیک، حفظ API قبلی، شش آزمون خودکار و CI چندنسخه‌ای |
| انتشار | ساخت خودکار EXE مستقل ویندوز، artifact قابل دریافت و انتشار خودکار با تگ `v*` به همراه checksum SHA-256 |

> **هشدار کاربردی:** این نسخه پخش بار DC متعادل را انجام می‌دهد و ولتاژ AC، توان راکتیو، اتصال کوتاه، حفاظت و پایداری دینامیکی را محاسبه نمی‌کند. از نتایج آن برای کنترل بلادرنگ یا تصمیم عملیاتی بدون مطالعات تأییدشده استفاده نکنید.

## تأییدهای پیش از انتشار

پیش از انتشار عمومی، باید اجرای سبز workflow، شماره نسخه، یادداشت انتشار، ایجاد صحیح EXE و checksum، و وضعیت مجوز بررسی شوند. امضای کد Authenticode در نسخهٔ تجاری توصیه می‌شود تا Windows SmartScreen و مشتریان سازمانی بتوانند ناشر را احراز کنند.

## برنامهٔ کوتاه‌مدت

انتشار بعدی باید روی پخش بار AC، ورود دادهٔ جدولی استاندارد، گزارش PDF امضاشده، سناریوهای N-2 و کنترل دسترسی نقش‌محور تمرکز کند. پس از آن، واردسازی CIM/CGMES، اقدام اصلاحی بهینه‌شده و یکپارچگی EMS/DMS می‌تواند به‌صورت ماژول تجاری عرضه شود.
